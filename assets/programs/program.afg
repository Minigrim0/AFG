fn turn(angle) {
    set initial_rotation = $Rotation + 100;
    set current_rot = $Rotation - initial_rotation;
        set current_rot = $Rotation - initial_rotation;
    }
    return;
}

fn main() {
    // Change velocity to 500
    set $VelocityY = 500;

    loop {
        if $Ray3Type != 0 {
            if $Ray3Dist <= 1000 {
                set $VelocityY = 0;
                call turn(90);
                set $VelocityY = 500;
            }
        }
    }
}
